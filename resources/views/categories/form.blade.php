<form method="POST" action="{{$action}}">@csrf @if($method!=='POST') @method($method) @endif<label class="form-label">Name</label><input name="name" class="form-control mb-3" value="{{old('name',$category->name??'')}}" required><label class="form-label">Description</label><textarea name="description" class="form-control mb-3">{{old('description',$category->description??'')}}</textarea><button class="btn btn-primary">{{$button}}</button> <a href="{{route('categories.index')}}" class="btn btn-light">Cancel</a></form>
