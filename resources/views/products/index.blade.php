@extends('layouts.app',['title'=>'Products']) @section('content')<div class="d-flex justify-content-between mb-3"><div><h2>Products</h2><p class="text-muted">Manage SKUs, pricing, categories and stock.</p></div><a href="{{route('products.create')}}" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Add Product</a></div><div class="card p-3 mb-3"><form class="row g-2"><div class="col-md-6"><input name="q" value="{{$q}}" class="form-control" placeholder="Search SKU or product..."></div><div class="col-auto"><button class="btn btn-outline-secondary">Search</button></div></form></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>SKU</th><th>Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody>@foreach($products as $p)<tr><td>{{$p->sku}}</td><td><strong>{{$p->name}}</strong><br><small class="text-muted">{{$p->description}}</small></td><td>{{$p->category->name}}</td><td>₱{{number_format($p->price,2)}}</td><td>{{$p->stock_quantity}}</td><td><span class="badge {{$p->stock_quantity===0?'bg-danger':($p->stock_quantity<=5?'bg-warning text-dark':'bg-success')}}">{{$p->stock_status}}</span></td><td class="text-end"><a href="{{route('products.edit',$p)}}" class="btn btn-sm btn-outline-primary">Edit</a>@if(auth()->user()->role!=='Viewer')<form method="POST" action="{{route('products.destroy',$p)}}" class="d-inline" onsubmit="return confirm('Delete this product?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</td></tr>@endforeach</tbody></table></div><div class="p-3">{{$products->links()}}</div></div>@endsection
