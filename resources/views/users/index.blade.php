@extends('layouts.app',['title'=>'Users']) @section('content')<div class="d-flex justify-content-between mb-3"><div><h2>Users</h2><p class="text-muted">Manage Admin, Editor and Viewer accounts.</p></div><a href="{{route('users.create')}}" class="btn btn-primary">+ Add User</a></div><div class="card"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach($users as $u)<tr><td>{{$u->name}}</td><td>{{$u->email}}</td><td><span class="badge badge-soft">{{$u->role}}</span></td><td><span class="badge {{$u->status==='Active'?'bg-success':'bg-secondary'}}">{{$u->status}}</span></td><td><a href="{{route('users.edit',$u)}}" class="btn btn-sm btn-outline-primary">Edit</a><form class="d-inline" method="POST" action="{{route('users.destroy',$u)}}" onsubmit="return confirm('Delete user?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@endforeach</tbody></table><div class="p-3">{{$users->links()}}</div></div>@endsection
