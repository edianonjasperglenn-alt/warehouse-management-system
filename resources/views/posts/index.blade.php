@extends('layouts.app',['title'=>'Announcements']) @section('content')<div class="d-flex justify-content-between mb-3"><div><h2>Announcements</h2><p class="text-muted">Internal posts and warehouse notices.</p></div><a href="{{route('posts.create')}}" class="btn btn-primary">+ New Post</a></div><div class="card"><table class="table align-middle mb-0"><thead><tr><th>Title</th><th>Author</th><th>Status</th><th>Views</th><th>Actions</th></tr></thead><tbody>@foreach($posts as $p)<tr><td><a href="{{route('posts.show',$p)}}" class="text-decoration-none">{{$p->title}}</a></td><td>{{$p->author->name}}</td><td><span class="badge {{$p->status==='Published'?'bg-success':'bg-secondary'}}">{{$p->status}}</span></td><td>{{$p->views}}</td><td><a href="{{route('posts.edit',$p)}}" class="btn btn-sm btn-outline-primary">Edit</a><form class="d-inline" method="POST" action="{{route('posts.destroy',$p)}}" onsubmit="return confirm('Delete post?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@endforeach</tbody></table><div class="p-3">{{$posts->links()}}</div></div>@endsection
